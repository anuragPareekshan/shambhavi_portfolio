document.addEventListener('DOMContentLoaded',function(){let currentZoom=1;let currentImages=[];let currentIndex=0;document.addEventListener('keydown',function(e){if(e.key==='Escape'){closeFullscreen();closeVideoModal()}
const overlay=document.getElementById('fullscreen-overlay1');if(overlay.style.display==='flex'){if(e.key==='ArrowRight'){nextImage()}else if(e.key==='ArrowLeft'){prevImage()}}});updateCurrentImages();const filterParam=getUrlParameter('filter');if(filterParam){filterGallery(filterParam);setTimeout(()=>{const gallerySection=document.querySelector('.gallery-container');if(gallerySection){gallerySection.scrollIntoView({behavior:'smooth'})}},300)}});function getUrlParameter(name){name=name.replace(/[\[]/,'\\[').replace(/[\]]/,'\\]');var regex=new RegExp('[\\?&]'+name+'=([^&#]*)');var results=regex.exec(location.search);return results===null?'':decodeURIComponent(results[1].replace(/\+/g,' '))}
function zoomIn(){if(currentZoom<3){currentZoom+=0.25;applyZoom()}}
function zoomOut(){if(currentZoom>0.5){currentZoom-=0.25;applyZoom()}}
function resetZoom(){currentZoom=1;const image=document.getElementById('fullscreen-image');image.style.transform='scale(1)';image.style.left='0';image.style.top='0'}
function applyZoom(){const image=document.getElementById('fullscreen-image');image.style.transform=`scale(${currentZoom})`}
function filterGallery(category){const links=document.querySelectorAll('.filter-link');links.forEach(link=>{link.classList.remove('active');if(link.textContent.toLowerCase().includes(category)||(category==='all'&&link.textContent.includes('All'))){link.classList.add('active')}});const items=document.querySelectorAll('.gallery-item');items.forEach(item=>{if(category==='all'){item.classList.remove('hide')}else{if(item.classList.contains(category)){item.classList.remove('hide')}else{item.classList.add('hide')}}});updateCurrentImages()}
function openFullscreen(img){const overlay=document.getElementById('fullscreen-overlay1');const fullImg=document.getElementById('fullscreen-image');const caption=document.getElementById('fullscreen-caption');fullImg.src=img.src;caption.textContent=img.getAttribute('data-caption');overlay.style.display='flex';document.body.style.overflow='hidden';resetZoom();updateCurrentImages();currentIndex=currentImages.findIndex(image=>image.src===img.src)}
function closeFullscreen(){const overlay=document.getElementById('fullscreen-overlay1');const fullImg=document.getElementById('fullscreen-image');overlay.style.display='none';document.body.style.overflow='hidden';fullImg.style.transform='scale(1)';fullImg.style.left='0';fullImg.style.top='0'}
function updateCurrentImages(){currentImages=[];const visibleItems=document.querySelectorAll('.gallery-item:not(.hide) img');visibleItems.forEach(img=>{currentImages.push({src:img.src,caption:img.getAttribute('data-caption')})})}
function nextImage(){if(currentImages.length<=1)return;currentIndex=(currentIndex+1)%currentImages.length;const fullImg=document.getElementById('fullscreen-image');const caption=document.getElementById('fullscreen-caption');fullImg.src=currentImages[currentIndex].src;caption.textContent=currentImages[currentIndex].caption;resetZoom()}
function prevImage(){if(currentImages.length<=1)return;currentIndex=(currentIndex-1+currentImages.length)%currentImages.length;const fullImg=document.getElementById('fullscreen-image');const caption=document.getElementById('fullscreen-caption');fullImg.src=currentImages[currentIndex].src;caption.textContent=currentImages[currentIndex].caption;resetZoom()}
function openVideoModal(videoId){const modal=document.getElementById('video-modal');const iframe=document.getElementById('youtube-iframe');iframe.src=`https://www.youtube.com/embed/${videoId}?autoplay=1`;modal.style.display='block';document.body.style.overflow='hidden'}
function closeVideoModal(){const modal=document.getElementById('video-modal');const iframe=document.getElementById('youtube-iframe');iframe.src='';modal.style.display='none';document.body.style.overflow='auto'}